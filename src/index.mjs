import "./styles.css";

// .mjs is the extension for ES modules
// ES modules are the official standard format to package JavaScript code for reuse
// .mjs files are different from .js files in that they allow the use of import and export statements

document.getElementById("app").innerHTML = `
<h1>D3.js Chart Examples</h1>
`;
